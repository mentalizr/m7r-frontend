import {it} from "mocha";
import { expect } from "chai";

describe("StepContent", () => {

    // let stepContent: StepContent;

    beforeEach(() => {
        // stepContent = new StepContent("<div></div>");
    })
    it("first test", () => {
        // let refIds: IterableIterator<string> = stepContent.getRefIds();
        // expect(refIds).to.have.lengthOf(0);
    })
})